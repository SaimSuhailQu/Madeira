diff --git a/dlls/ntdll/loader.c b/dlls/ntdll/loader.c
index 0224441..d59e252 100644
--- a/dlls/ntdll/loader.c
+++ b/dlls/ntdll/loader.c
@@ -4122,7 +4122,9 @@ NTSTATUS CDECL wine_server_handle_to_fd( HANDLE handle, unsigned int access, int
  */
 static void iat_life_sweep( const char *when )
 {
+#ifdef __arm64ec__
     extern void *xlate_ios_jit( void *ptr );
+#endif
     static struct { const void *base; ULONG rva; } seen[64];
     static int nseen, sweeps, reports;
     LIST_ENTRY *list, *entry;
@@ -4169,7 +4171,11 @@ static void iat_life_sweep( const char *when )
                  * two views; a zero in EITHER is a branch-to-zero waiting to happen,
                  * and checking only the PE side (as the first cut of this probe did)
                  * cannot see a stale pool slot at all. */
+#ifdef __arm64ec__
                 pslot = xlate_ios_jit( slot );
+#else
+                pslot = slot;
+#endif
                 if (pslot && pslot != slot) { has_pool = 1; n_pool++; pool_val = *(ULONG_PTR *)pslot; }
 
                 if (pe_val && !(has_pool && !pool_val)) continue;   /* both views fine */
diff --git a/dlls/ntdll/ntdll.spec b/dlls/ntdll/ntdll.spec
index ae1335b..923f1c1 100644
--- a/dlls/ntdll/ntdll.spec
+++ b/dlls/ntdll/ntdll.spec
@@ -1752,7 +1752,7 @@
 @ extern -private __wine_syscall_dispatcher
 @ extern -private __wine_unix_call_dispatcher
 @ extern -private -arch=arm64ec __wine_unix_call_dispatcher_arm64ec
-@ extern -private -arch=arm64ec p_ios_jit_translate_addr
+@ extern -private -arch=arm64,arm64ec p_ios_jit_translate_addr
 @ extern -private -arch=arm64,arm64ec p_ios_jit_reverse_translate_addr
 @ extern -private ios_teb_tsd_offset
 @ extern -private __wine_unixlib_handle
diff --git a/dlls/ntdll/signal_arm64.c b/dlls/ntdll/signal_arm64.c
index 0224441..174b4ed 100644
--- a/dlls/ntdll/signal_arm64.c
+++ b/dlls/ntdll/signal_arm64.c
@@ -115,6 +115,20 @@ __ASM_GLOBAL_FUNC( RtlCaptureContext,
  * explorer at CoRegisterClassObject). Unix-side loader_ios.c fills the
  * pointer at ntdll load; NULL → identity. */
 #ifndef __arm64ec__
+void *p_ios_jit_translate_addr = NULL;
+
+void *__attribute__((naked)) xlate_ios_jit( void *ptr )
+{
+    asm( ".seh_proc xlate_ios_jit\n\t"
+         ".seh_endprologue\n\t"
+         "cbz x0, 1f\n\t"                                 /* NULL → return NULL */
+         "adrp x16, p_ios_jit_translate_addr\n\t"
+         "ldr x16, [x16, #:lo12:p_ios_jit_translate_addr]\n\t"
+         "cbz x16, 1f\n\t"                                /* fn-ptr unset → identity */
+         "br x16\n"                                       /* tail-call unix fn */
+         "1: ret\n\t"
+         ".seh_endproc" );
+}
 void *p_ios_jit_reverse_translate_addr = NULL;
 
 void *__attribute__((naked)) xlate_ios_jit_rev( void *ptr )
